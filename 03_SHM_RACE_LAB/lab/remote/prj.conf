# 03_SHM_RACE_LAB - core1 (appcpu) image
#
# No CONFIG_LOG / console here on purpose - see 01_HELLO_DUALCORE_LAB's
# doc for why (no UART console support on the appcpu image in Zephyr yet).
# This core proves what it's doing entirely through its heartbeat LED.
#
# NOTE: &ipm0 is intentionally left DISABLED - see prj.conf in the procpu
# app for why (only one of ipm0/mbox0 is ever enabled per lab).

CONFIG_GPIO=y
CONFIG_MBOX=y
